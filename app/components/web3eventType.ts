export type Web3event = {
    id: number,
    start_time: string,
    end_time: string,
    register_type: number,
    image: string,
    title: string,
    organizer: string,
    addr: string,
    click_num: number,
    is_collect: number,
    is_like: number
}